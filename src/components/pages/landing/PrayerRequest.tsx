import React from "react";

const PrayerRequest = () => {
  return (
    <section
      data-landing-sections
      id="prayer_request"
      className="h-screen bg-neutral-100 w-full scroll-mt-[6rem]"
    ></section>
  );
};

export default PrayerRequest;
